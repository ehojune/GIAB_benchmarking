# HARVEST — Yuan 수확 후보

프로젝트가 일단락되면 심사해서 `D:\Yuan` (github.com/ehojune/Yuan)에 PR한다. 규약 정본은 Yuan `AGENTS.md`.

| 후보 | 종류 | 왜 |
|---|---|---|
| ONT 중복(재베이스콜) 판정: read_id 집합 대조 | lesson | PacBio movie ID의 ONT 대응물. read_id(fast5/pod5 UUID)는 재베이스콜해도 안 바뀌고 sequencing_summary에 전부 있어 fastq를 훑지 않아도 된다. `phase2_ont/scripts/03_dup_evidence.sh` |
| Clair3/DeepVariant 모델 가용성 확인 절차 | lesson | "컨테이너에 모델이 있다"를 가정하면 조용히 틀린 모델로 돈다. 이미지 `/opt/models` 목록 ↔ 필요 모델 대조 후 HKU/Rerio에서 받아 마운트. `phase2_ont/scripts/01_prepare_login_node.sh`, `docs/reference/ont_pipeline_choice.md` |
| Nextflow 기본 셸에 pipefail 없음 | incident-note | `samtools fastq \| minimap2 \| samtools sort`는 앞단이 죽어도 성공으로 보인다. `process.shell = ['/bin/bash','-euo','pipefail']` |
| minimap2 `-y` + Guppy fastq 코멘트 | incident-note | `-y`는 uBAM에서 옮긴 태그를 살리는 옵션인데, Guppy fastq의 `runid=... ch=...` 코멘트에 쓰면 SAM aux 형식이 아니라 BAM이 깨진다 |
| `except FileNotFoundError` 는 "실행 파일 못 씀"을 다 못 잡는다 | incident-note | PATH 에 실행권한 없는 `bcftools` 가 있으면 exec 이 **PermissionError**(errno 13)를 낸다 — FileNotFoundError 의 형제일 뿐 하위 클래스가 아니라 핸들러를 그냥 지나쳐 스크립트가 통째로 죽었다. `except OSError` 로 잡아야 둘 다 덮인다 (errno 13/2/8/20 전부 OSError). 2026-09-22 실측 traceback |
| 로그인 노드에 CLI 가 없는 클러스터: 도구 해석을 한 곳으로 모은다 | lesson | 계산은 컨테이너로 도는데 리뷰 스크립트만 맨 이름(`bcftools`)으로 불러 크래시했다. `$TOOL` -> 파이프라인 config 의 `container_<name>` -> PATH 순으로 찾는 헬퍼 하나를 두면 **파이프라인이 실제로 쓴 판과 같은 것**이 보장된다 — 판이 다르면 변이 수·ts/tv 가 미묘하게 갈린다. `phase{1,2}/scripts/35_review_qc.py` 의 `tool_img()`/`bcftools_cmd()` |
| 실측값을 적을 때 **측정 조건**을 같이 적는다 | lesson | truvari maxvmem 을 "ONT 67~73 GB" 로 적었는데 그게 4스레드 값인 줄 몰라 8스레드 HiFi 값과 비교됐고, "데이터셋이 메모리를 두 배로 가른다"는 없는 결론이 섰다. 같은 스레드로 재니 같은 대역이었다. 조건 없는 실측값은 나중에 반드시 다른 조건의 값과 비교된다 (2026-09-22) |
| 외부가 같은 입력으로 낸 수치를 대조군으로 확보한다 | lesson | ONT 가 같은 플로우셀에 자기 hap.py 결과를 공개해 둬서, 우리 재정렬 파이프라인이 그 값을 F1 0.001 이내로 재현하는지 볼 수 있었다. 대조군 하나가 나머지 14런의 신뢰도까지 받친다 — 데이터를 고를 때 "외부 결과가 같이 공개돼 있는가"를 선택 기준에 넣을 값어치가 있다 (2026-09-22) |
| 추정을 직접 측정으로 바꾸면 **추정으로는 안 보이던 것**이 보인다 | lesson | ts/tv 구간 밖 값을 가정 깔고 빼서 1.33 으로 추정했고 실측도 1.3064 였다 — 숫자는 안 바뀌었다. 대신 빼기 산수로는 나올 수 없는 둘이 보였다: 베이스콜러가 좋을수록 구간 밖 ts/tv 가 **낮고**, 구간 밖에서 두 caller 의 순 차이가 germline 신호(2.18)를 띤다. **측정의 값어치는 숫자 정정이 아니라 관측 가능한 양이 늘어나는 것**이다 (2026-09-22) |
| 벤치마크 구간 기반 결론은 **구간 밖에 대해 아무 말도 안 한다** | lesson | hap.py 로 "DeepVariant 가 Clair3 를 이긴다"를 얻었는데, 구간 밖에서는 DV 가 86k 콜을 덜 부르고 그 순차분 ts/tv 가 2.18(germline 신호)이다. truth 가 구간 안에만 있으니 구간 밖 성능은 **측정된 적이 없다**. 반복영역 등 구간 밖을 쓰는 용도로 넘어갈 때 결론을 그대로 들고 가면 안 된다 (2026-09-22) |
| 두 이름 체계를 key 로 잇는 곳은 **외부 유래 데이터에서 조용히 끊긴다** | incident-note | 카탈로그 행을 `(sample, 경로 basename)` 으로 run_table 과 이었다. GIAB 에서 받은 행은 디렉토리 이름이 곧 dataset 이라 성립했지만, 외부에서 들여온 행은 **경로가 배포처 구조를, dataset 이 우리 명명 규칙을 따라** 달랐다. 채점까지 끝난 런이 카탈로그에 미처리로 남았고 신호는 경고 한 줄뿐이었다. 자동 매칭 대신 명시적 별칭표로 고쳤다. **정상 경고와 비정상 경고가 같은 줄로 나오면 아무도 안 본다** (2026-09-23) |
| `ont-wgs` 파이프라인 (minimap2/Clair3/Sniffles2/LongPhase) | nextflow-pipeline | 첫 실행 검증 후 판단. 검증 전에는 올리지 않는다 |
| SGE 잡 1개 + Nextflow local executor 패턴 | lesson | 노드 수 제한이 있고 계산 노드에 외부망이 없는 클러스터의 정석. phase1/phase2가 같은 형태를 두 번 썼다 |
| ONT 매핑 건강도는 리드 개수가 아니라 염기 기준 | lesson | ONT 릴리스에 섞인 짧은 fail 리드가 reads-mapped%를 39%까지 끌어내리는데 정렬은 정상이다. 미매핑/매핑 리드 평균 길이비로 가른다 (실측 7~12배). 근거: `docs/reference/2026-08-27-ont-qc-first-pass.md` |
| DeepVariant raw 카운트는 RefCall로 1.7~3.4배 부풀어 있다 | lesson | PASS만 세면 Clair3와 3~7% 일치. caller 일치 판정을 raw로 하면 전건 오탐. phase1·phase2에서 각각 확인 |
| R9 ONT indel 정확도는 베이스콜러 버전이 지배한다 | lesson | guppy 3.2.x 1.75~1.97M vs 4.2.2 544~551k (3.5배). R9 indel 분석은 4.2.2+ 데이터셋만 |
| `crawl_release.py` — GIAB FTP 디렉토리 인덱스 재귀 크롤 + 파일별 HEAD → manifest diff | script | `current.tree`가 2025-02-27 이후 갱신되지 않아(2026-09-16 확인) 새 벤치마크(HG002 v5.0q 등)가 manifest에서 통째로 빠졌다. 인덱스 크기는 반올림이라 HEAD가 필수. NCBI는 동시 12+16 스레드에서 503 — 3+4 스레드·지수 백오프로 통과 |
| GIAB `latest/`는 심링크가 아니라 복사본이고 내용이 바뀐다 | lesson | HG002 latest/가 2026-05-27 v4.2.1→v5.0q로 교체됐다. 로컬 미러에는 두 버전이 공존하므로 평가는 버전 디렉토리를 직접 지정한다 |
| GIAB FTP와 S3 미러는 양방향으로 어긋난다 | lesson | HiSeq300x FASTQ 7천여 개는 FTP 사본이 S3보다 15% 작고(재압축), Strand-Seq EMBL·PacBio MtSinai 등 2,173 파일은 FTP에서만 사라졌다(S3에는 있음). manifest 크기는 다운로드 소스(S3) 기준이라 FTP만 보고 갱신하면 verify 전부 불일치·재다운로드가 난다. 크롤러는 FTP 불일치를 S3 HEAD로 교차 확인한다 (`crawl_data.py`, 2026-09-16) |
| GIAB `data/` 인덱스 기반 선별 HEAD 크롤 | script | 파일 7만 개를 전부 HEAD하지 않고 인덱스의 수정일·근사 크기로 신규/변경 후보만 고른다. 2026-08-13 manifest는 S3 목록 기반이라 FTP에만 있던 HG008 NIST 트리·Verkko·HG009 신규 등 15,605 파일(19.7 TiB)이 빠져 있었다 (`crawl_data.py`) |
| 카탈로그 파생 필드는 파일명 토큰으로 추정할 때 GIAB 용어를 조심 | lesson | `smvar`/`stvar`는 GIAB germline 벤치마크의 small/structural variant이지 somatic이 아니다. `phased` 단어는 purple 등 somatic 산출물 파일명에도 나온다. 인덱스 유무는 "어떤 인덱스라도 있음"이 아니라 BAM/CRAM/VCF 각각의 사이드카 짝으로 세야 PARTIAL이 잡힌다 (Codex 리뷰 지적, `catalog_new_dirs.py`) |
| 안 받기로 한 데이터는 manifest에서 지우는 것만으로 남지 않는다 | lesson | 크롤러에 exclusion 목록이 없으면 카탈로그 행이 살아 있는 한 다음 크롤이 "기존 디렉토리의 신규 파일"로 되돌려 넣는다. 목록 파일(`declined_by_decision.tsv`) + 크롤러 가드 + 카탈로그 생성기 필터 세 곳에 남겨야 결정이 유지된다 (2026-09-17) |
| GIAB S3 미러는 2024년 이후 somatic 신규 데이터를 거의 안 받는다 | lesson | HG008 NIST 트리·HG009 신규·Verkko를 표본 96건 조사해 S3 200 응답은 1건, 용량 상위 25건은 0건. `TOOL=s3`이어도 전부 FTP fallback이라 83 MB/s 실측치로 일정을 잡으면 크게 틀린다. 미러 가용성은 다운로드 계획 전에 표본으로 확인할 것 |
| 같은 데이터가 GIAB FTP에 다른 이름으로 두 번 올라와 있을 수 있다 | incident-note | `superseded-2022-data/BCM_Illumina_WGS_20220816/`와 `BCM_ILMN-somatic-analysis_20220816/`는 파일명·크기가 26건 모두 동일하다. 크롤이 새 이름을 신규로 잡아 1.63 TiB를 다시 받을 뻔했다. 대용량 신규 디렉토리는 형제 디렉토리와 파일명+크기를 대조해볼 것 |
| FASTQ 헤더로 시퀀싱 플랫폼 추정 (`detect_fastq_platform.sh`) | script | 리드 이름 규약만으로 판별 — Illumina 7필드 콜론, PacBio `m<무비명>/<zmw>/(ccs\|범위)`(Sequel/Revio 신형 + RS-II 구형 모두), ONT UUID·`runid=`, MGI `LxCxRx`. Illumina면 기기ID 접두어(`LH00`→NovaSeq X/X Plus 등 12종, 비공식 관례)로 구체 기종까지 추정. 헤더 불일치 시 리드 길이(<1000bp 숏리드/≥1000bp 롱리드)로 폴백. GIAB 특정 로직이 없어 다른 프로젝트에도 그대로 재사용 가능 |
| `pipefail` 켜진 채 `\| \| true`로 파이프라인 실패를 넘기면 PIPESTATUS가 사라진다 | lesson | `set -eo pipefail` 상태에서 `a \| b \| \| true`가 실행되면, pipefail이 `a\|b`를 실패로 판정해 `\|\|`의 우변(`true`)이 별도 파이프라인으로 도는데 PIPESTATUS가 그 `true` 하나짜리로 덮어써진다(bash 5.3.15 확인). SIGPIPE(141)처럼 "기대된 실패"만 걸러내고 싶으면 `\|\| true`가 아니라 그 구간만 `set +o pipefail` / `set -o pipefail`로 감싸 PIPESTATUS를 직접 읽을 것. `detect_fastq_platform.sh`에서 손상된 gzip이 조용히 exit 0으로 새는 걸 이 방식으로 잡음 |
| GIAB README가 HG002-only 데이터셋의 트리오 짝을 HPRC S3로 가리킬 수 있다 | lesson | `Illumina_PCRfree_downsampled/README`(403 B)가 "HG002, HG003, and HG004"라고 쓰고 HPRC 링크만 준다. 파일은 `s3://human-pangenomics/working/HPRC_PLUS/HG002/raw_data/Illumina/parents/`. HG002-only로 보이는 디렉토리는 README의 외부 링크를 따라가 볼 것 (2026-09-17) |
| Google brain-genomics-public 버킷에 HG001~HG007 NovaSeq/HiSeq X PCR-free·PCR-plus 20~50x FASTQ 계열이 있다 | reference | 업체 프로덕션(NovaSeq 6000, 2x151, PCR-free, 30x)과 조건이 같은 공개 raw. 로그인 없이 HTTPS, md5는 GCS JSON API `md5Hash`(base64). 경로 `research/sequencing/fastq/{novaseq,hiseqx,novaseqx}/`. GIAB FTP 밖이라 카탈로그에 없다 |
| 원격 gz FASTQ는 HTTP range 256 KB로 리드 길이·장비·플로우셀을 읽을 수 있다 | lesson | `curl -r 0-262143 URL \| zcat \| head` — 잘린 gzip도 앞부분은 풀린다. 4 MB 표본의 bases/압축바이트 비로 깊이도 추정된다(문서 100x 데이터셋에서 111x). GIAB S3·HPRC S3·GCS 모두 range 지원. BGISEQ500이 PE100인 것, MGISEQ PCR-free가 ~70x인 것을 이걸로 알았다 |
| `codex exec`는 비대화형 Bash에서 `< /dev/null` 없이 멎는다 | incident-note | "Reading additional input from stdin..."에서 무한 대기. 프롬프트를 인수로 줘도 stdin을 읽으려 한다 (2026-09-17, 자문 1회 유실 후 재실행) |
| 탭 구분 매니페스트를 `IFS=$'\t' read`로 읽으면 빈 칸이 접혀 필드가 밀린다 | incident-note | 탭은 IFS 공백류라 연속 구분자가 하나로 합쳐진다. md5 칸이 빈 HPRC 행 4개가 `kind` 자리에 note를 받아 조용히 건너뛰어졌다(VERIFY_ONLY 스모크 테스트에서 10건 중 6건만 나와 발견). `awk -F'\t' -v OFS=$'\x1f'`로 바꿔 `IFS=$'\x1f'`로 읽으면 빈 칸이 보존된다. phase2 `02_fetch_external_reads.sh`는 빈 칸이 없어 우연히 무사하다 (2026-09-17) |
| 벤치마크 대상은 truth set 유무가 아니라 "truth 종류"로 갈린다 | lesson | HG008에 draft benchmark가 있어도 `somatic-stvar`/`CNV`라 단일 샘플 germline VCF 평가에는 못 쓴다. 그 결과 phase2에서 DeepVariant는 한 번도 벤치마크되지 않는다 — DV가 도는 런(R10)과 germline truth가 있는 샘플(HG001~007=R9)이 겹치지 않기 때문 (`phase2_ont/scripts/60_benchmark.sh`, 2026-09-17) |
| 플랫폼별 파이프라인은 caller 목록을 전역 기본값으로 두면 안 된다 | lesson | ONT는 R9.4.1에 DeepVariant 모델이 없어 런마다 caller가 다르다. 제출·QC·벤치마크 세 스크립트가 모두 `run_table.tsv`의 `dv_model` 열을 같은 축으로 읽게 맞췄다. 전역 `CALLERS="deepvariant clair3"`(phase1 방식)를 그대로 옮기면 R9 런에서 없는 VCF를 찾다 실패한다 |
| 같은 변이 종류라도 truth set은 레퍼런스 판마다 따로 있다 | lesson | SV 벤치마크로 가장 널리 인용되는 `HG002_SVs_Tier1_v0.6`은 **GRCh37 전용**이다. GRCh38 정렬 결과에 쓰려면 `HG002_GRCh38_v5.0q_stvar`(T2T-Q100 유래)로 가야 한다. 도구·샘플이 맞아도 레퍼런스가 안 맞으면 못 쓴다 — truth 후보는 이름이 아니라 파일명의 레퍼런스 토큰으로 거를 것 (`phase2_ont/scripts/61_benchmark_sv.sh`, 2026-09-17) |
| 벤치마크 매칭 파라미터는 자체 판단보다 발행처 지정값을 쓴다 | lesson | Truvari는 `-r`/`-C`/`--pick`/`--refine`만 바꿔도 SV precision·recall이 크게 흔들린다. GIAB v5.0q README가 지정한 명령을 그대로 쓰면 수치를 남에게 설명할 수 있다. 단 README의 `-C 5000`은 chunksize지 sizemin이 아니다 — 소스(`truvari/bench.py`, `variant_params.py`)로 확인할 것 |
| `local a=$1 b=...$a...` 는 빈 값을 쓴다 | incident-note | `local`은 빌트인이라 인자를 **전부 먼저 확장**한 뒤 대입한다. 같은 `local` 줄에서 앞 변수를 참조하면 그 시점엔 아직 미설정이라 빈 문자열이 들어가고, `set -u`면 "unbound variable"까지 난다. 캐시 경로가 `.noast.vcf.gz`로 뭉개져 발견 — 줄을 나눠야 한다 (2026-09-17) |
| truvari refine 기본 정렬기 poa는 머신 간 비결정적이다 | lesson | truvari 문서가 명시한다. 벤치마크 수치를 재현 가능한 기록으로 남기려면 `--align mafft`로 고정해야 한다. 그리고 refine은 변이 표현을 바꾸므로 refine 전/후를 둘 다 기록해야 수치가 얼마나 움직였는지 판단할 수 있다 |
| 생성하는 스크립트는 생성물에도 문법 검사를 돌려야 한다 | lesson | 잡 스크립트 본문을 `${var:+여러 줄}` 로 끼워 넣으면 확장 과정의 quote removal이 따옴표를 벗겨 `echo "(align=poa)"` 의 괄호가 노출된다 — 생성기는 멀쩡한데 생성물이 문법 에러다. 분기는 생성물 안의 `if` 로 넣고, `bash -n <생성된 잡>` 을 검증에 포함할 것 (2026-09-17) |
| 오래 걸리는 잡은 시작할 때 기존 결과를 지우면 안 된다 | lesson | 출력 디렉토리가 있으면 거부하는 도구(truvari bench 등) 때문에 잡 앞에 `rm -rf`를 두기 쉽다. 그러면 잡이 죽거나 중복 제출됐을 때 멀쩡한 이전 결과만 날아간다. 잡별 임시 디렉토리에서 돌고 성공 후 옮기면, 덤으로 **중간에 죽은 실행이 반쪽 산출물을 남겨 "완료"로 오판되는 것**도 같이 막힌다 |
| 짝이 되는 파일 둘을 각각 mv 해서 공개하면 섞인 세대가 관측된다 | lesson | VCF와 .tbi 를 따로 rename하면 동시 실행이 엇갈려 A의 본체에 B의 인덱스가 붙는다. bcftools는 헤더에 실행 명령을 적으므로 같은 입력이어도 바이트가 달라 오프셋이 어긋나고, **죽지 않고 조용히 엉뚱한 레코드를 읽는다.** 짝을 디렉토리에 담아 디렉토리를 통째로 rename할 것 — 원자적이고 락이 필요 없다 (2026-09-17) |
| `mv src dst` 는 dst가 디렉토리면 그 안으로 넣고 성공을 낸다 | incident-note | 결과 디렉토리를 제자리로 옮기는 코드에서 목적지가 이미 있으면 한 단계 중첩된 채 "완료"가 찍힌다. 의도가 "이 이름으로 놓기"라면 `mv -T` 를 쓸 것 — 그 경우 실패한다 |
| unquoted heredoc 안의 백틱은 주석에 있어도 실행된다 | incident-note | 잡 스크립트를 `<<EOF` 로 생성할 때 주석에 적은 `` `[ -n ... ] && rm` `` 이 **생성 시점에** 명령 치환으로 돌았다. 생성물에 남기고 싶은 백틱은 `<<'EOF'` 를 쓰거나 이스케이프할 것 (2026-09-17) |
| 미리보기 모드(DRY)에 정리 코드가 섞이면 미리보기가 파괴적이 된다 | incident-note | 잔여물 청소를 DRY 조기 반환보다 앞에 두어 `DRY=1` 이 `rm -rf` 를 돌리고 있었다. 부수효과가 있는 코드는 전부 DRY 가드 **뒤**에 둘 것 (2026-09-17) |
| 잔여물 청소의 생존 판정은 작업 단위가 아니라 잡 번호로 한다 | lesson | "이 dsid로 사는 잡이 없다"는 jobid 파일에 기대므로, 기록이 실패한 잡은 살아 있어도 죽은 것으로 보인다. 그 상태에서 청소하면 실행 중인 잡의 작업 디렉토리를 지운다. 디렉토리 이름에 잡 번호를 박아 두고 qstat 에서 그 번호를 직접 확인할 것 |
| Illumina FASTQ 링크·이름은 read group(unit)이 아니라 파일 쌍 단위로 유일해야 한다 | incident-note | 2x250 unit이 레인뿐이라 레인당 분할 파일(_001~_017)과 라이브러리 2개(S1/S2)가 같은 링크 이름으로 겹쳐 첫 실행이 3/8 데이터셋에서 멈췄다. 분할 번호(`_R1_(\d+)`)를 덧붙이고 만들기 전에 Counter로 유일성 검사. `phase3_shortread_wgs/scripts/03_make_pipeline_dirs.py` (2026-09-18) |
| `zcat f \| head -1` 한 줄이 pipefail+errexit 스크립트를 141로 죽인다 — 이미 HARVEST에 있던 함정을 다시 밟았다 | incident-note | concat.sh 마지막 "첫 리드 확인"에서 head가 먼저 끝나 zcat이 SIGPIPE. 병합(cat·mv)은 끝난 뒤라 파일은 정상인데 러너가 15샘플 전부 FAIL로 집계했다. `( zcat f \|\| true ) \| head -1`로 고침. 위 detect_fastq_platform 항목과 같은 뿌리 — **`set -eo pipefail` 스크립트에서 소비자가 먼저 끝나는 파이프(head, grep -q)는 전부 의심할 것** (2026-09-18) |
| 미러 가용성 표본으로 다운로드 소요 시간을 추정하지 않는다 | lesson | GIAB 신규 데이터가 S3에 없어(표본 96건 중 1건) FTP fallback 속도를 예상했는데, JOBS=8 실측은 평균 92 MB/s로 S3 실측(83 MB/s)보다 빨랐다. 8.67 TiB가 28시간 49분에 들어왔다. 표본은 경로를 알려주지 대역폭을 알려주지 않는다 (2026-09-18, `docs/runs/2026-09-18-phase0-download-complete.md`) |
| verify.sh 카테고리 합계 = manifest 합계가 완료 판정의 기계적 근거 | lesson | 12개 카테고리 출력의 files·GiB를 더하면 manifest 총계와 정확히 같아야 한다(2026-09-18 81,302 files / 116,509.5 GiB 일치). 100.0% 표시만 믿지 말고 합계를 맞춰본다 |
| 변이 "개수"로 추정한 품질 격차는 truth 대비에서 몇 배로 커질 수 있다 | lesson | ONT R9 indel에서 베이스콜러 격차를 PASS 개수로 3.5배로 봤는데, hap.py로 재니 여분이 거의 전부 FP여서 **FP 기준 42배**였다. 개수는 TP와 FP를 섞어 보여주므로 격차를 과소평가한다 — truth가 있으면 개수 비교를 결론으로 삼지 말 것 (2026-09-18) |
| precision과 recall은 같은 원인으로 안 움직인다 | lesson | 베이스콜러 버전을 올리면 ONT R9 indel precision은 6.3배 좋아지는데 recall은 1.8배에 그치고 .75에서 멈춘다. 전처리 개선이 고치는 것은 FP고, 플랫폼이 애초에 못 부르는 것은 남는다. 한쪽 지표만 보고 "개선됐다"고 적지 말 것 |
| 낮은 genome-wide ts/tv는 콜러가 아니라 신뢰구간 밖 콜일 수 있다 (**2026-09-22 직접 측정으로 확인**: 구간 안 2.097~2.140 / 밖 1.092~1.484, 10건) | lesson | 전 코호트 ts/tv가 1.86~1.97로 낮아 FP 과다를 의심했는데, hap.py로 보니 벤치마크 구간 안 SNP precision이 .990~.997이었다. genome-wide 카운트의 약 23%가 구간 밖이고 거기 ts/tv가 ~1.33이면 전체가 설명된다(**전체 SNV 수로 가중**하면 1.15가 나오는데 틀린 값이다 — ts/tv의 분모는 Tv이므로 Tv로 가중하거나 Ti/Tv 카운트를 복원해 빼야 한다. 가중평균 자체가 문제가 아니다). **구간 안과 밖을 갈라 보기 전에는 genome-wide ts/tv로 파이프라인을 의심하지 말 것** |
| SGE h_vmem은 예약도 상한도 아닐 수 있다 | incident-note | nbb2에서 `-l h_vmem=32G`로 제출한 잡이 maxvmem 73 GB를 쓰고도 exit 0으로 끝났다. consumable=NO는 "예약 안 함"만 뜻하는 줄 알았는데 kill도 안 한다. 노드 과점유를 막는 건 슬롯뿐이다 — 값은 문서로서 실측에 맞춰 두되 그것에 기대지 말 것 (2026-09-18) |
| whatshap stats의 `block_n50` 열은 N50이 아니라 NG50이다 | incident-note | 유전체 길이 기준이라 **블록 span 합이 유전체의 절반에 못 미치면 정의상 0**을 낸다(콘티그 길이를 모를 때만 nan). 0을 "미산출"로 읽고 경고 라벨을 달았다가 실제로는 "위상 블록이 절반도 못 덮는다"는 측정값임을 뒤늦게 알았다. 50% 지점에서 절벽이라 0과 정상값 사이에 중간이 없다 (2026-09-19) |
| 검증 스탬프 파일에는 검증한 값을 적어야 한다 | lesson | 대용량 파일 md5를 매번 다시 계산하지 않으려 `<파일>.md5ok` 스탬프를 뒀는데, 존재 여부만 보면 매니페스트의 기대 md5가 바뀌어도(항목 정정, 같은 경로의 새 판) 낡은 스탬프가 통과시킨다. 스탬프에 값을 적고 비교할 것 — 테스트로 잡았다 |
| ONT 공개 데이터(ont-open-data)에 GIAB 7샘플 R10.4.1이 통째로 있다 | reference | `s3://ont-open-data/giab_2025.01/` — HG001~HG007, SQK-LSK114 kit14, Dorado v0.8.2 `dna_r10.4.1_e8.2_400bps@v5.0.0`, 샘플당 PromethION 2셀(~90x), POD5 + 정렬 BAM, 익명 다운로드(eu-west-1). ONT 자체 hap.py(7샘플)·Truvari(HG002) 결과도 같은 버킷에 있어 외부 대조군이 된다. **CC BY-NC 4.0(비상업)**. GIAB FTP에는 HG002 R10 ONT가 없으므로 R10 벤치마크를 하려면 여기가 사실상 유일한 출처다 |
| 프로세스 치환으로 공급하는 루프는 공급자 실패가 안 올라온다 | lesson | `while read ...; done < <(awk ...)` 에서 awk가 죽어도 rc에도 pipefail에도 잡히지 않아 "0건 처리 후 성공"이 된다. `set -e`를 켜도 마찬가지다. **읽은 행 수를 세어 기대 건수와 비교**하는 게 실질적인 방어다 (2026-09-19) |
| 샘플시트가 가리키는 보조 파일도 준비 상태 판정에 넣어야 한다 | lesson | aligned_bam 진입이 `.bai`를 명시적으로 넘기는데 inputs_manifest에는 BAM만 넣어 뒀더니, 인덱스가 없는 상태를 ready로 보고 잡을 제출해 파이프라인 입구에서 죽는 경로가 생겼다. "입력"은 리드 파일만이 아니다 |
| 스모크 테스트는 "진입 타입 수"만큼 시트가 필요하다 | lesson | 파이프라인이 fastq·uBAM·aligned_bam 셋을 지원하는데 테스트 시트 하나에 섞어 넣으면 **타입별 분기를 못 탄다** — "전부 aligned_bam 이면 인덱싱 생략" 같은 조건은 섞인 시트에서 영영 거짓이다. 단독 시트를 타입마다 따로 두고 stub를 여러 번 돌릴 것(각 1분). phase2에서 aligned_bam 경로가 실제 데이터 제출 직전까지 한 번도 실행된 적 없던 것을 이렇게 발견했다 (2026-09-21) |
| 채널을 재그룹할 때 meta 필드를 하드코딩하면 조용히 유실된다 | incident-note | Nextflow에서 unit 단위로 다시 묶으며 `type: 'ubam'`을 리터럴로 적어 뒀더니, 새로 만든 진입 타입이 그 지점에서 uBAM으로 둔갑했다. 재그룹 키에 넣어야 살아남는다. 그룹 키는 "묶는 기준"이자 "살려서 넘길 값" 목록이다 (2026-09-21) |
| 남이 정렬한 BAM을 비교군에 섞으면 비교축에 교란이 박힌다 | lesson | 케미스트리 비교(R9 vs R10)를 하려고 데이터를 들여오면서 그 하나만 배포처 정렬을 쓰면, 차이가 케미스트리 때문인지 정렬 때문인지 못 가른다. 같은 도구(minimap2)라도 마찬가지다. 비교가 목적인 데이터는 **파이프라인 진입점을 나머지와 맞추는 것이 비용보다 우선**한다 |
| ts/tv 같은 비(比)를 **분모가 아닌 수**로 가중평균하면 틀린다 | incident-note | 구간 밖 ts/tv를 `(전체×N_전체 − 안×N_안)/N_밖` 로 구해 1.15를 적었다. ts/tv = Ti/Tv 의 분모는 **Tv**인데 전체 SNV 수로 가중했다. `Ti = N·r/(1+r)`, `Tv = N−Ti` 로 카운트를 복원해 빼면 **1.33**이다(Tv로 가중해도 같다). 비 자체가 못 섞이는 게 아니라 **가중치를 그 비의 분모로 잡아야** 한다 — 백분율은 분모 가중평균으로 정확히 합쳐지고, F1은 집계 의미가 또 달라 둘 다 따로 봐야 한다. 틀린 값이 문서 5곳에 퍼진 뒤 외부 리뷰로 잡혔다 (2026-09-21) |
| 도구 스레드를 SGE 슬롯에 묶으면 슬롯 기반 패킹이 무력해진다 | lesson | `-t ${NSLOTS}` 로 넘기면 "슬롯을 올려 노드당 동시 실행 수를 줄인다"가 약해진다 — 잡당 메모리가 같이 올라 노드 총량이 기대만큼 안 준다. **슬롯=패킹, 스레드=잡당 메모리는 별개 축이니 따로 노출할 것.** 단 얼마나 주는지는 도구마다 재야 한다 — 아래 항목 참고 (2026-09-22) |
| 잡당 메모리가 스레드에 비례한다고 가정하면 과소 추정한다 | incident-note | truvari refine 을 같은 데이터셋으로 `-t 22 -> 8` 하니 스레드 64% 감소에 메모리는 **29~46%만** 줄었다(192.6→136.2 / 177.3→96.5 / 188.1→121.0 GB). `base + k·threads` 로 풀면 base 가 50~104 GB — **가장 큰 영역 하나의 작업 공간**이 스레드와 무관하게 남는 것으로 보인다(ENOMEM 때 실패한 할당이 정확히 64 GiB 한 방이었던 것과 맞는다). 초판에서 "비례한다"고 적었던 건 phase2 ONT(-t 8)와 phase1 HiFi(-t 22)를 비교한 것이라 **데이터셋 차이가 섞인 대조**였다. 스레드를 바꿔 볼 거면 같은 입력으로 짝지어 재고, 최악값을 못 내리면 제어 수단은 결국 동시 실행 수다 (2026-09-22) |
| 워커가 죽어도 잡은 `r` 상태로 슬롯을 영원히 붙잡을 수 있다 | incident-note | abPOA가 malloc 실패를 찍고 abort하지 않아 부모가 pool join에서 멎었다. SGE는 정상 실행으로 보고 qacct에도 기록이 없다. **판별 지문**: `qstat -j <id> \| grep usage` 를 60초 간격으로 두 번 떠서 `cpu=` 가 안 움직이고, `vmem` 이 `maxvmem` 보다 한참 낮으면(3.2G vs 149G) 워커가 죽고 메모리를 놓은 것이다. 작업 디렉토리 크기도 같이 본다 (2026-09-22) |
| `qstat -f` 는 "쓸 수 있는 노드"를 알려주지 않는다 | lesson | 클러스터 전 큐 인스턴스가 다 나온다(nbb2 실측 25개). 그중 우리 몫이 넷이라는 건 SGE ACL이 아니라 사람끼리의 약속이라 스크립트가 알 길이 없다. 큐×호스트 교차 검사는 **오타·폐기된 이름**만 막는다(그것도 값어치는 있다 — 없는 노드를 지정하면 잡이 에러 없이 qw로 영원히 남는다). 허용 목록은 물어서 설정에 적는 수밖에 없다 (2026-09-22) |
| 벤치마크 도구의 recall 분자와 precision 분모를 섞지 말 것 | incident-note | truvari에서 `comp cnt`(우리가 부른 수, precision 분모)를 "정답 중 찾은 수"로 잘못 읽어 "28,123 중 20,600 발견"이라고 적었다. recall 분자는 `TP-base`(21,282~22,147)다. **검산법**: `TP-base/base cnt` 가 보고된 recall 과, `TP-comp/comp cnt` 가 precision 과 일치하는지 본다. `TP-base ≠ TP-comp` 인 것 자체가 매칭이 1:1이 아니라는 신호다 (2026-09-22, 외부 리뷰) |
| 기기 세대·메타데이터를 디렉토리 이름으로 추정하면 비교축이 조용히 망가진다 | lesson | `HG002.PacBio_CCS_10kb`/`_15kb` 는 이름에 세대 표시가 없는데 실제로는 m54=Sequel I 다. 이름 기반 판정이 둘을 Sequel II로 묶어, 그 라벨을 축으로 쓰는 스크립트의 결론을 틀리게 만들 뻔했다. 권위 있는 토큰은 리드 파일명의 movie ID 다(m84=Revio, m64=Sequel II, m54=Sequel I, m14/m15=RS II). 경로의 `PBmixSequel<NNN>` 은 세대 표시가 아니다 (2026-09-22, 외부 리뷰) |
| Bash 툴 heredoc의 백슬래시 뭉갬 — 같은 사고를 두 번 냈다 | incident-note | 에이전트가 `<<'PY'` heredoc으로 셸 텍스트를 생성할 때 `\n`(줄 연속)이 리터럴 `\n` 이나 진짜 개행으로 뭉개진다. `echo "$jid" > f \n \|\| {...}` 가 되어 jobid 파일에 `900123 n` 이 들어갔고 중복 제출 가드가 무력해졌다. **`bash -n` 은 통과한다** — 문법은 멀쩡하다. 2026-08-27에 겪고 "이스케이프 든 셸 텍스트는 편집 툴로"라고 정해 놓고 2026-09-22에 또 밟았다. 재발 방지는 규칙이 아니라 **백슬래시가 필요 없는 형태**(`if ! cmd; then`)로 쓰는 것 |
| 빠르게 끝나는 벤치마크 잡은 실패가 "성공처럼" 보인다 | lesson | truvari 5런이 4분씩이라 qstat을 두 번 떠 보는 사이에 셋이 시작하고 끝났다. "잡이 사라졌다 = 죽었다"로 읽기 쉬운데 `qacct` 가 갈라 준다 — **실행되고 끝난 잡만** 기록되므로, 기록이 있고 `exit=0` 이면 완주, 기록이 없는데 qstat에 있으면 아직 실행 중, 둘 다 없으면 큐에서 지워진 것이다 (2026-09-22) |
| 취득 경로가 갈라지면 반영이 빠진다 | lesson | 데이터 출처가 4곳(FTP·ENA·ONT공개·GCS/S3)으로 갈라진 뒤, 새 데이터를 들여온 PR이 카탈로그·README·Excel 반영을 빠뜨렸다(HG002 R10.4.1, 160 GiB). 취득 진입점을 하나로 모으면 '받는 목록'이 곧 '반영 대상 목록'이 된다. `phase0_download/scripts/fetch_all.sh` |
| '받지 않기로 한 목록'이 다운로드 대상이 되는 구멍 | incident-note | manifest를 파일명으로 받는 범용 분기가 있으면 declined/stale 목록도 이름만으로 매치된다. 게다가 그 목록은 3열(경로/바이트/사유)이라 2열 파서의 크기 비교가 전건 실패한다 — 10.7 TiB를 받고 전부 MISMATCH. 이름 거부 가드: `phase0_download/scripts/download.sh` |
| 독립된 두 실행이 스트림의 거의 같은 지점에서 죽으면 blind retry를 멈춘다 | lesson | MGISEQ HG002 재정렬이 원본 kbb 파이프라인(09-21)과 복구 스크립트 1차 시도(09-22)에서 각각 SAM 948,868,188·948,884,480번째 줄에서 죽었다 — 레코드 수 차이 0.0017%. 스크립트도 날짜도 다른데 위치가 이렇게 가까우면 무작위 스트림 손상보다 특정 리드(또는 그 리드 처리 방식)의 재현 가능한 문제일 가능성이 높다. 같은 자원으로 재시도해 재현되는지부터 보고, 재현되면 그때 record-level(QNAME 기준) 조사로 넘어간다 — SAM 줄 수를 2로 나눠 FASTQ 위치를 추정하지 말 것(secondary/supplementary가 섞여 수백만 단위로 틀어질 수 있다). `phase3_shortread_wgs`, [docs/runs/2026-09-23-phase3-input-audit-and-mgiseq-repair.md](docs/runs/2026-09-23-phase3-input-audit-and-mgiseq-repair.md) (2026-09-23, 외부 리뷰) |
| 층화 재채점은 `Subset=*` 를 기존 채점과 대조해 입력 어긋남을 잡는다 | pattern | hap.py 를 `--stratification` 으로 다시 돌리면 전체 행(`*`)은 기존 unstratified 결과와 TP/FN/FP 가 같아야 한다. 이 등식이 "같은 VCF·truth·BED·엔진으로 돌았다"의 기계 검증이 된다 — 62_tstv_regions 의 안+밖=전체 대조와 같은 발상(합이 맞는지로 분할의 온전성을 본다). `phase1_pacbio_hifi/scripts/60_benchmark.sh collect_strat` |
| 로그인 노드용 점검 스크립트를 qsub 로 감싸는 래퍼 | code | 전 런 `--pass-counts`·ts/tv 는 한두 시간이라 로그인 노드를 붙잡는다. `printf %q` 로 명령을 그대로 잡 스크립트에 박고, 큐·노드 해석은 제출 스크립트와 같은 헬퍼, `-w` 로 hold_jid. `phase1_pacbio_hifi/scripts/qsub_task.sh` |
| 출력 크기 = 입력 합 검증은 동시 실행을 못 잡는다 | lesson | cat 병합의 완료 판정을 크기로 했는데, 같은 샘플에 병합 잡이 둘 뜨면 두 cat이 같은 `.part`에 쓰고 최종 크기는 여전히 입력 합과 같다 — 내용이 섞여도 검증을 통과한다. 크기 검증은 '덜 썼다'만 잡는다. 동시 실행은 따로 막을 것: `mkdir <dir>.lock`(Lustre에서도 원자적; flock은 마운트 옵션에 따라 안 됨) + 제출 쪽에서 jobid 기록 후 qstat로 중복 제출 거부. `phase3_shortread_wgs/scripts/03_make_pipeline_dirs.py` (2026-09-23) |
| *(정정, 2026-09-23)* "추정을 직접 측정으로 바꾸면 추정으로는 안 보이던 것이 보인다" 행의 **둘째 관찰은 틀렸다** | incident-note | 그 행이 "구간 밖 두 caller 의 순 차이가 germline 신호(2.18)를 띤다" 를 측정의 소득으로 들었다. 차집합을 직접 세니 빼기의 부산물이었다(아래 "순 차이" 행). 그 행의 교훈은 **첫째 관찰(베이스콜러가 좋을수록 구간 밖 ts/tv 가 낮다)만으로** 선다. 수확할 때 둘째 관찰은 빼고 옮길 것 |
| *(정정, 2026-09-23)* "벤치마크 구간 기반 결론은 구간 밖에 대해 아무 말도 안 한다" 행의 **예시가 틀렸다** | incident-note | 그 행이 "DV 가 구간 밖에서 86k 콜을 덜 부르고 그 순차분 ts/tv 가 2.18(germline 신호)" 을 예로 들었는데 부산물이었다. **교훈 자체는 여전히 맞다** — 구간 밖에 대해선 ts/tv 같은 간접 지표밖에 없고, 그 지표로는 FP 와 진짜를 못 가른다. 수확할 때 예시를 이 정정으로 바꿀 것 |
| 두 집합의 **순 차이**는 어느 집합에도 없는 신호를 만든다 | incident-note | 두 caller 의 구간 밖 순 차이 ts/tv 가 2.18 이라 "DV 가 진짜 변이를 버린다" 를 의심했다. `bcftools isec` 로 실제 차집합을 세니 Clair3 단독 0.87 · DV 단독 0.67 — 둘 다 공유 콜(1.31)보다 낮고 germline 같은 집합은 없었다. DV 단독이 더 tv 쪽이라 빼면 ts 가 남았을 뿐이다. 순 차이는 두 집합의 **구성 차이**를 재지 원소가 무엇인지를 재지 않는다. **차집합을 직접 셀 수 있으면 빼기로 추론하지 않는다.** 단, 이것은 "버리지 않는다" 의 증명이 아니다 — 의심의 근거가 사라졌을 뿐 질문은 truth 가 있어야 닫힌다(첫 판에서 "둘 다 FP 지배" 로 분류했다가 Codex 리뷰로 거뒀다) (2026-09-23, `phase2_ont/scripts/63_caller_diff_regions.sh`) |
| hap.py 결과만 있어도 **query caller 를 알아낼 수 있다** | lesson | hap.py(vcfeval)는 query VCF 의 `##FILTER` 헤더를 출력에 옮긴다. caller 마다 필터 **설명 문구**가 다르다 — Clair3 `LowQual="Low quality variant"`·`RefCall="Reference call"`, DeepVariant `RefCall="Genotyping model thinks this site is reference."`. 공개 벤치마크의 caller 가 명시돼 있지 않을 때 출력 VCF 첫 BGZF 블록(64 KB)만 받아 가를 수 있다. **검색 부재는 증거가 아니다** — GitHub 코드 검색은 DV 에서 문구를 못 찾았지만 원본 파일엔 있었다 (2026-09-23) |
| 게이트를 풀 때는 **그 게이트가 막던 이유**를 다시 읽는다 | incident-note | `DUP_OK=1` 로 dup 런을 풀면서 run_table 의 dataset·entry·모델·크기 열만 보고 note 열을 안 읽었다. HG002 guppy-V2.3.4 의 note 에 "HG001 오라벨 플로우셀 3개가 섞여 있다 — 걸러야 한다" 가 적혀 있었다. HG001/HG002 혼합 샘플을 16시간 돌리고 그 위에 채점까지 자동으로 얹을 뻔했다(PR #39 Codex 리뷰가 잡아 시작 전 취소). **게이트는 이유를 잊게 만든다** — 막혀 있는 동안은 아무도 이유를 볼 일이 없다. 나머지 릴리스는 run_id 대조로 검증했다(양성 대조 V2.3.4 26/26 검출, V3.4.5·V3.2.4 0) (2026-09-23) |
| 도는 bash 스크립트를 덮어쓰지 않는다 | lesson | bash는 스크립트를 실행하면서 조금씩 읽는다. 생성기를 다시 돌려 실행 중인 `concat.sh`를 잘라 다시 쓰면 그 bash가 새 파일의 엉뚱한 오프셋부터 읽는다(내용이 같아도 자르는 순간 빈 파일을 볼 수 있다). 생성기는 실행 중 표지(lock)가 있으면 건너뛰고, 내용이 같으면 쓰지 말 것. `phase3_shortread_wgs/scripts/03_make_pipeline_dirs.py` `write_if_changed` (2026-09-23) |
| 벤치마크 **구간 밖** 성능은 더 넓은 truth 로 **층화해서** 직접 잰다 | lesson | v4.2.1 밖에서 DV 가 진짜 변이를 버리는지를 ts/tv 로는 못 가렸다. HG002 v5.0q smvar(T2T-Q100) 로 채점하되 v5.0q 결과에서 v4.2.1 결과를 빼지 않고 **한 채점 안에서** `hap.py --stratification` 으로 겹침/v5.0q에만 층을 갈랐다(두 truth 는 겹침에서도 달라 빼면 truth 차이가 섞인다). 겹침 층이 v4.2.1 채점과 0.001 안에서 맞는지를 대조로 두면 "밖" 층을 믿을 수 있다. 결과: DV 가 v4.2.1 밖에서도 FN·FP 둘 다 적다 (2026-09-24, `phase2_ont/scripts/64_v5q_strata.py`) |
| 남성 샘플의 **성염색체를 다른 층과 섞지 않는다** — 반접합 유전형 불일치가 F1 0.06 으로 보인다 | incident-note | v4.2.1 은 chr1~22 만 덮어, 가르지 않으면 v5.0q 의 chrX·Y 전체가 "v4.2.1 밖" 에 들어간다. 따로 떼어 보니 두 caller 모두 F1 0.054~0.065 인데 **FN ≈ FP ≈ (전체 − TP)** 로 대칭 — 놓친 게 아니라 같은 자리의 유전형이 어긋난 모양이다(haploid truth vs diploid 호출). 섞었으면 어려운 영역 결론을 이 인공 수치가 끌어내렸을 것이다. **FN 과 FP 가 같으면 먼저 표현 불일치를 의심한다** (2026-09-24) |
| ts/tv 는 **방향이라도** FP 판정 근거로 쓰지 않는다 | lesson | 구간 밖 단독 콜의 ts/tv 가 Clair3 0.87 > DV 0.67 이라 "Clair3 단독이 덜 FP 같다" 를 크기 없이 방향으로만 적었는데, 나중에 truth 로 세니 Clair3 의 FP 가 DV 의 두 배 이상이었다. 범위·집합(단독 콜 vs 전체 FP)이 달라 엄밀한 반례는 아니지만, ts/tv 가 가리킨 방향을 truth 가 뒷받침하지 않았다. ts/tv 는 집합에 **어떤 치환이 섞였나**를 재지 FP 가 **얼마나** 있는지를 재지 않는다 (2026-09-24) |
