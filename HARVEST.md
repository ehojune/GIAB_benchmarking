# HARVEST — Yuan 수확 후보

프로젝트가 일단락되면 심사해서 `D:\Yuan` (github.com/ehojune/Yuan)에 PR한다. 규약 정본은 Yuan `AGENTS.md`.

| 후보 | 종류 | 왜 |
|---|---|---|
| ONT 중복(재베이스콜) 판정: read_id 집합 대조 | lesson | PacBio movie ID의 ONT 대응물. read_id(fast5/pod5 UUID)는 재베이스콜해도 안 바뀌고 sequencing_summary에 전부 있어 fastq를 훑지 않아도 된다. `phase2_ont/scripts/03_dup_evidence.sh` |
| Clair3/DeepVariant 모델 가용성 확인 절차 | lesson | "컨테이너에 모델이 있다"를 가정하면 조용히 틀린 모델로 돈다. 이미지 `/opt/models` 목록 ↔ 필요 모델 대조 후 HKU/Rerio에서 받아 마운트. `phase2_ont/scripts/01_prepare_login_node.sh`, `docs/reference/ont_pipeline_choice.md` |
| Nextflow 기본 셸에 pipefail 없음 | incident-note | `samtools fastq \| minimap2 \| samtools sort`는 앞단이 죽어도 성공으로 보인다. `process.shell = ['/bin/bash','-euo','pipefail']` |
| minimap2 `-y` + Guppy fastq 코멘트 | incident-note | `-y`는 uBAM에서 옮긴 태그를 살리는 옵션인데, Guppy fastq의 `runid=... ch=...` 코멘트에 쓰면 SAM aux 형식이 아니라 BAM이 깨진다 |
| `ont-wgs` 파이프라인 (minimap2/Clair3/Sniffles2/LongPhase) | nextflow-pipeline | 첫 실행 검증 후 판단. 검증 전에는 올리지 않는다 |
| SGE 잡 1개 + Nextflow local executor 패턴 | lesson | 노드 수 제한이 있고 계산 노드에 외부망이 없는 클러스터의 정석. phase1/phase2가 같은 형태를 두 번 썼다 |
| ONT 매핑 건강도는 리드 개수가 아니라 염기 기준 | lesson | ONT 릴리스에 섞인 짧은 fail 리드가 reads-mapped%를 39%까지 끌어내리는데 정렬은 정상이다. 미매핑/매핑 리드 평균 길이비로 가른다 (실측 7~12배). 근거: `docs/reference/2026-08-27-ont-qc-first-pass.md` |
| DeepVariant raw 카운트는 RefCall로 1.7~3.4배 부풀어 있다 | lesson | PASS만 세면 Clair3와 3~7% 일치. caller 일치 판정을 raw로 하면 전건 오탐. phase1·phase2에서 각각 확인 |
| R9 ONT indel 정확도는 베이스콜러 버전이 지배한다 | lesson | guppy 3.2.x 1.75~1.97M vs 4.2.2 544~551k (3.5배). R9 indel 분석은 4.2.2+ 데이터셋만 |
| `crawl_release.py` — GIAB FTP 디렉토리 인덱스 재귀 크롤 + 파일별 HEAD → manifest diff | script | `current.tree`가 2025-02-27 이후 갱신되지 않아(2026-09-16 확인) 새 벤치마크(HG002 v5.0q 등)가 manifest에서 통째로 빠졌다. 인덱스 크기는 반올림이라 HEAD가 필수. NCBI는 동시 12+16 스레드에서 503 — 3+4 스레드·지수 백오프로 통과 |
| GIAB `latest/`는 심링크가 아니라 복사본이고 내용이 바뀐다 | lesson | HG002 latest/가 2026-05-27 v4.2.1→v5.0q로 교체됐다. 로컬 미러에는 두 버전이 공존하므로 평가는 버전 디렉토리를 직접 지정한다 |
| GIAB FTP와 S3 미러는 양방향으로 어긋난다 | lesson | HiSeq300x FASTQ 7천여 개는 FTP 사본이 S3보다 15% 작고(재압축), Strand-Seq EMBL·PacBio MtSinai 등 2,173 파일은 FTP에서만 사라졌다(S3에는 있음). manifest 크기는 다운로드 소스(S3) 기준이라 FTP만 보고 갱신하면 verify 전부 불일치·재다운로드가 난다. 크롤러는 FTP 불일치를 S3 HEAD로 교차 확인한다 (`crawl_data.py`, 2026-09-16) |
| GIAB `data/` 인덱스 기반 선별 HEAD 크롤 | script | 파일 7만 개를 전부 HEAD하지 않고 인덱스의 수정일·근사 크기로 신규/변경 후보만 고른다. 2026-08-13 manifest는 S3 목록 기반이라 FTP에만 있던 HG008 NIST 트리·Verkko·HG009 신규 등 15,605 파일(19.7 TiB)이 빠져 있었다 (`crawl_data.py`) |
| 카탈로그 파생 필드는 파일명 토큰으로 추정할 때 GIAB 용어를 조심 | lesson | `smvar`/`stvar`는 GIAB germline 벤치마크의 small/structural variant이지 somatic이 아니다. `phased` 단어는 purple 등 somatic 산출물 파일명에도 나온다. 인덱스 유무는 "어떤 인덱스라도 있음"이 아니라 BAM/CRAM/VCF 각각의 사이드카 짝으로 세야 PARTIAL이 잡힌다 (Codex 리뷰 지적, `catalog_new_dirs.py`) |
| 안 받기로 한 데이터는 manifest에서 지우는 것만으로 남지 않는다 | lesson | 크롤러에 exclusion 목록이 없으면 카탈로그 행이 살아 있는 한 다음 크롤이 "기존 디렉토리의 신규 파일"로 되돌려 넣는다. 목록 파일(`declined_by_decision.tsv`) + 크롤러 가드 + 카탈로그 생성기 필터 세 곳에 남겨야 결정이 유지된다 (2026-09-17) |
| GIAB S3 미러는 2024년 이후 somatic 신규 데이터를 거의 안 받는다 | lesson | HG008 NIST 트리·HG009 신규·Verkko를 표본 96건 조사해 S3 200 응답은 1건, 용량 상위 25건은 0건. `TOOL=s3`이어도 전부 FTP fallback이라 83 MB/s 실측치로 일정을 잡으면 크게 틀린다. 미러 가용성은 다운로드 계획 전에 표본으로 확인할 것 |
| 같은 데이터가 GIAB FTP에 다른 이름으로 두 번 올라와 있을 수 있다 | incident-note | `superseded-2022-data/BCM_Illumina_WGS_20220816/`와 `BCM_ILMN-somatic-analysis_20220816/`는 파일명·크기가 26건 모두 동일하다. 크롤이 새 이름을 신규로 잡아 1.63 TiB를 다시 받을 뻔했다. 대용량 신규 디렉토리는 형제 디렉토리와 파일명+크기를 대조해볼 것 |
| FASTQ 헤더로 시퀀싱 플랫폼 추정 (`detect_fastq_platform.sh`) | script | 리드 이름 규약만으로 판별 — Illumina 7필드 콜론, PacBio `m<무비명>/<zmw>/(ccs\|범위)`(Sequel/Revio 신형 + RS-II 구형 모두), ONT UUID·`runid=`, MGI `LxCxRx`. Illumina면 기기ID 접두어(`LH00`→NovaSeq X/X Plus 등 12종, 비공식 관례)로 구체 기종까지 추정. 헤더 불일치 시 리드 길이(<1000bp 숏리드/≥1000bp 롱리드)로 폴백. GIAB 특정 로직이 없어 다른 프로젝트에도 그대로 재사용 가능 |
| `pipefail` 켜진 채 `\| \| true`로 파이프라인 실패를 넘기면 PIPESTATUS가 사라진다 | lesson | `set -eo pipefail` 상태에서 `a \| b \| \| true`가 실행되면, pipefail이 `a\|b`를 실패로 판정해 `\|\|`의 우변(`true`)이 별도 파이프라인으로 도는데 PIPESTATUS가 그 `true` 하나짜리로 덮어써진다(bash 5.3.15 확인). SIGPIPE(141)처럼 "기대된 실패"만 걸러내고 싶으면 `\|\| true`가 아니라 그 구간만 `set +o pipefail` / `set -o pipefail`로 감싸 PIPESTATUS를 직접 읽을 것. `detect_fastq_platform.sh`에서 손상된 gzip이 조용히 exit 0으로 새는 걸 이 방식으로 잡음 |
| GIAB README가 HG002-only 데이터셋의 트리오 짝을 HPRC S3로 가리킬 수 있다 | lesson | `Illumina_PCRfree_downsampled/README`(403 B)가 "HG002, HG003, and HG004"라고 쓰고 HPRC 링크만 준다. 파일은 `s3://human-pangenomics/working/HPRC_PLUS/HG002/raw_data/Illumina/parents/`. HG002-only로 보이는 디렉토리는 README의 외부 링크를 따라가 볼 것 (2026-09-17) |
| Google brain-genomics-public 버킷에 HG001~HG007 NovaSeq/HiSeq X PCR-free·PCR-plus 20~50x FASTQ 계열이 있다 | reference | 업체 프로덕션(NovaSeq 6000, 2x151, PCR-free, 30x)과 조건이 같은 공개 raw. 로그인 없이 HTTPS, md5는 GCS JSON API `md5Hash`(base64). 경로 `research/sequencing/fastq/{novaseq,hiseqx,novaseqx}/`. GIAB FTP 밖이라 카탈로그에 없다 |
| 원격 gz FASTQ는 HTTP range 256 KB로 리드 길이·장비·플로우셀을 읽을 수 있다 | lesson | `curl -r 0-262143 URL \| zcat \| head` — 잘린 gzip도 앞부분은 풀린다. 4 MB 표본의 bases/압축바이트 비로 깊이도 추정된다(문서 100x 데이터셋에서 111x). GIAB S3·HPRC S3·GCS 모두 range 지원. BGISEQ500이 PE100인 것, MGISEQ PCR-free가 ~70x인 것을 이걸로 알았다 |
| `codex exec`는 비대화형 Bash에서 `< /dev/null` 없이 멎는다 | incident-note | "Reading additional input from stdin..."에서 무한 대기. 프롬프트를 인수로 줘도 stdin을 읽으려 한다 (2026-09-17, 자문 1회 유실 후 재실행) |
| 탭 구분 매니페스트를 `IFS=$'\t' read`로 읽으면 빈 칸이 접혀 필드가 밀린다 | incident-note | 탭은 IFS 공백류라 연속 구분자가 하나로 합쳐진다. md5 칸이 빈 HPRC 행 4개가 `kind` 자리에 note를 받아 조용히 건너뛰어졌다(VERIFY_ONLY 스모크 테스트에서 10건 중 6건만 나와 발견). `awk -F'\t' -v OFS=$'\x1f'`로 바꿔 `IFS=$'\x1f'`로 읽으면 빈 칸이 보존된다. phase2 `02_fetch_external_reads.sh`는 빈 칸이 없어 우연히 무사하다 (2026-09-17) |
| 벤치마크 대상은 truth set 유무가 아니라 "truth 종류"로 갈린다 | lesson | HG008에 draft benchmark가 있어도 `somatic-stvar`/`CNV`라 단일 샘플 germline VCF 평가에는 못 쓴다. 그 결과 phase2에서 DeepVariant는 한 번도 벤치마크되지 않는다 — DV가 도는 런(R10)과 germline truth가 있는 샘플(HG001~007=R9)이 겹치지 않기 때문 (`phase2_ont/scripts/60_benchmark.sh`, 2026-09-17) |
| 플랫폼별 파이프라인은 caller 목록을 전역 기본값으로 두면 안 된다 | lesson | ONT는 R9.4.1에 DeepVariant 모델이 없어 런마다 caller가 다르다. 제출·QC·벤치마크 세 스크립트가 모두 `run_table.tsv`의 `dv_model` 열을 같은 축으로 읽게 맞췄다. 전역 `CALLERS="deepvariant clair3"`(phase1 방식)를 그대로 옮기면 R9 런에서 없는 VCF를 찾다 실패한다 |
| 같은 변이 종류라도 truth set은 레퍼런스 판마다 따로 있다 | lesson | SV 벤치마크로 가장 널리 인용되는 `HG002_SVs_Tier1_v0.6`은 **GRCh37 전용**이다. GRCh38 정렬 결과에 쓰려면 `HG002_GRCh38_v5.0q_stvar`(T2T-Q100 유래)로 가야 한다. 도구·샘플이 맞아도 레퍼런스가 안 맞으면 못 쓴다 — truth 후보는 이름이 아니라 파일명의 레퍼런스 토큰으로 거를 것 (`phase2_ont/scripts/61_benchmark_sv.sh`, 2026-09-17) |
| 벤치마크 매칭 파라미터는 자체 판단보다 발행처 지정값을 쓴다 | lesson | Truvari는 `-r`/`-C`/`--pick`/`--refine`만 바꿔도 SV precision·recall이 크게 흔들린다. GIAB v5.0q README가 지정한 명령을 그대로 쓰면 수치를 남에게 설명할 수 있다. 단 README의 `-C 5000`은 chunksize지 sizemin이 아니다 — 소스(`truvari/bench.py`, `variant_params.py`)로 확인할 것 |
| `local a=$1 b=...$a...` 는 빈 값을 쓴다 | incident-note | `local`은 빌트인이라 인자를 **전부 먼저 확장**한 뒤 대입한다. 같은 `local` 줄에서 앞 변수를 참조하면 그 시점엔 아직 미설정이라 빈 문자열이 들어가고, `set -u`면 "unbound variable"까지 난다. 캐시 경로가 `.noast.vcf.gz`로 뭉개져 발견 — 줄을 나눠야 한다 (2026-09-17) |
| truvari refine 기본 정렬기 poa는 머신 간 비결정적이다 | lesson | truvari 문서가 명시한다. 벤치마크 수치를 재현 가능한 기록으로 남기려면 `--align mafft`로 고정해야 한다. 그리고 refine은 변이 표현을 바꾸므로 refine 전/후를 둘 다 기록해야 수치가 얼마나 움직였는지 판단할 수 있다 |
| 생성하는 스크립트는 생성물에도 문법 검사를 돌려야 한다 | lesson | 잡 스크립트 본문을 `${var:+여러 줄}` 로 끼워 넣으면 확장 과정의 quote removal이 따옴표를 벗겨 `echo "(align=poa)"` 의 괄호가 노출된다 — 생성기는 멀쩡한데 생성물이 문법 에러다. 분기는 생성물 안의 `if` 로 넣고, `bash -n <생성된 잡>` 을 검증에 포함할 것 (2026-09-17) |
| 오래 걸리는 잡은 시작할 때 기존 결과를 지우면 안 된다 | lesson | 출력 디렉토리가 있으면 거부하는 도구(truvari bench 등) 때문에 잡 앞에 `rm -rf`를 두기 쉽다. 그러면 잡이 죽거나 중복 제출됐을 때 멀쩡한 이전 결과만 날아간다. 잡별 임시 디렉토리에서 돌고 성공 후 옮기면, 덤으로 **중간에 죽은 실행이 반쪽 산출물을 남겨 "완료"로 오판되는 것**도 같이 막힌다 |
| 짝이 되는 파일 둘을 각각 mv 해서 공개하면 섞인 세대가 관측된다 | lesson | VCF와 .tbi 를 따로 rename하면 동시 실행이 엇갈려 A의 본체에 B의 인덱스가 붙는다. bcftools는 헤더에 실행 명령을 적으므로 같은 입력이어도 바이트가 달라 오프셋이 어긋나고, **죽지 않고 조용히 엉뚱한 레코드를 읽는다.** 짝을 디렉토리에 담아 디렉토리를 통째로 rename할 것 — 원자적이고 락이 필요 없다 (2026-09-17) |
| `mv src dst` 는 dst가 디렉토리면 그 안으로 넣고 성공을 낸다 | incident-note | 결과 디렉토리를 제자리로 옮기는 코드에서 목적지가 이미 있으면 한 단계 중첩된 채 "완료"가 찍힌다. 의도가 "이 이름으로 놓기"라면 `mv -T` 를 쓸 것 — 그 경우 실패한다 |
| unquoted heredoc 안의 백틱은 주석에 있어도 실행된다 | incident-note | 잡 스크립트를 `<<EOF` 로 생성할 때 주석에 적은 `` `[ -n ... ] && rm` `` 이 **생성 시점에** 명령 치환으로 돌았다. 생성물에 남기고 싶은 백틱은 `<<'EOF'` 를 쓰거나 이스케이프할 것 (2026-09-17) |
| 미리보기 모드(DRY)에 정리 코드가 섞이면 미리보기가 파괴적이 된다 | incident-note | 잔여물 청소를 DRY 조기 반환보다 앞에 두어 `DRY=1` 이 `rm -rf` 를 돌리고 있었다. 부수효과가 있는 코드는 전부 DRY 가드 **뒤**에 둘 것 (2026-09-17) |
| 잔여물 청소의 생존 판정은 작업 단위가 아니라 잡 번호로 한다 | lesson | "이 dsid로 사는 잡이 없다"는 jobid 파일에 기대므로, 기록이 실패한 잡은 살아 있어도 죽은 것으로 보인다. 그 상태에서 청소하면 실행 중인 잡의 작업 디렉토리를 지운다. 디렉토리 이름에 잡 번호를 박아 두고 qstat 에서 그 번호를 직접 확인할 것 |
| Illumina FASTQ 링크·이름은 read group(unit)이 아니라 파일 쌍 단위로 유일해야 한다 | incident-note | 2x250 unit이 레인뿐이라 레인당 분할 파일(_001~_017)과 라이브러리 2개(S1/S2)가 같은 링크 이름으로 겹쳐 첫 실행이 3/8 데이터셋에서 멈췄다. 분할 번호(`_R1_(\d+)`)를 덧붙이고 만들기 전에 Counter로 유일성 검사. `phase3_shortread_wgs/scripts/03_make_pipeline_dirs.py` (2026-09-18) |
| `zcat f \| head -1` 한 줄이 pipefail+errexit 스크립트를 141로 죽인다 — 이미 HARVEST에 있던 함정을 다시 밟았다 | incident-note | concat.sh 마지막 "첫 리드 확인"에서 head가 먼저 끝나 zcat이 SIGPIPE. 병합(cat·mv)은 끝난 뒤라 파일은 정상인데 러너가 15샘플 전부 FAIL로 집계했다. `( zcat f \|\| true ) \| head -1`로 고침. 위 detect_fastq_platform 항목과 같은 뿌리 — **`set -eo pipefail` 스크립트에서 소비자가 먼저 끝나는 파이프(head, grep -q)는 전부 의심할 것** (2026-09-18) |
| 미러 가용성 표본으로 다운로드 소요 시간을 추정하지 않는다 | lesson | GIAB 신규 데이터가 S3에 없어(표본 96건 중 1건) FTP fallback 속도를 예상했는데, JOBS=8 실측은 평균 92 MB/s로 S3 실측(83 MB/s)보다 빨랐다. 8.67 TiB가 28시간 49분에 들어왔다. 표본은 경로를 알려주지 대역폭을 알려주지 않는다 (2026-09-18, `docs/runs/2026-09-18-phase0-download-complete.md`) |
| verify.sh 카테고리 합계 = manifest 합계가 완료 판정의 기계적 근거 | lesson | 12개 카테고리 출력의 files·GiB를 더하면 manifest 총계와 정확히 같아야 한다(2026-09-18 81,302 files / 116,509.5 GiB 일치). 100.0% 표시만 믿지 말고 합계를 맞춰본다 |
| 변이 "개수"로 추정한 품질 격차는 truth 대비에서 몇 배로 커질 수 있다 | lesson | ONT R9 indel에서 베이스콜러 격차를 PASS 개수로 3.5배로 봤는데, hap.py로 재니 여분이 거의 전부 FP여서 **FP 기준 42배**였다. 개수는 TP와 FP를 섞어 보여주므로 격차를 과소평가한다 — truth가 있으면 개수 비교를 결론으로 삼지 말 것 (2026-09-18) |
| precision과 recall은 같은 원인으로 안 움직인다 | lesson | 베이스콜러 버전을 올리면 ONT R9 indel precision은 6.3배 좋아지는데 recall은 1.8배에 그치고 .75에서 멈춘다. 전처리 개선이 고치는 것은 FP고, 플랫폼이 애초에 못 부르는 것은 남는다. 한쪽 지표만 보고 "개선됐다"고 적지 말 것 |
| 낮은 genome-wide ts/tv는 콜러가 아니라 신뢰구간 밖 콜일 수 있다 | lesson | 전 코호트 ts/tv가 1.86~1.97로 낮아 FP 과다를 의심했는데, hap.py로 보니 벤치마크 구간 안 SNP precision이 .990~.997이었다. genome-wide 카운트의 약 23%가 구간 밖이고 거기 ts/tv가 ~1.15면 전체가 설명된다. **구간 안과 밖을 갈라 보기 전에는 genome-wide ts/tv로 파이프라인을 의심하지 말 것** |
| SGE h_vmem은 예약도 상한도 아닐 수 있다 | incident-note | nbb2에서 `-l h_vmem=32G`로 제출한 잡이 maxvmem 73 GB를 쓰고도 exit 0으로 끝났다. consumable=NO는 "예약 안 함"만 뜻하는 줄 알았는데 kill도 안 한다. 노드 과점유를 막는 건 슬롯뿐이다 — 값은 문서로서 실측에 맞춰 두되 그것에 기대지 말 것 (2026-09-18) |
